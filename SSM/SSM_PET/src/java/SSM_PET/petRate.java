import  java.io.*;
import  java.util.*;
import java.time.LocalDateTime;

public class petRate
{
    
    public petRate() { }
    
    public petRate(petRate toCopy, boolean copyAll) // copy constructor 
    {
        if (copyAll)
        {
        }
    }
}